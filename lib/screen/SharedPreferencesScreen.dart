import 'package:flutter/material.dart';

class SharedPreferencesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Practice - shared_preferences"),
      ),
      body: Center(
        child: Text('Practice - shared_preferences')
      ),
    );
  }
}