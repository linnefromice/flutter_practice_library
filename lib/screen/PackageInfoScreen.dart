import 'package:flutter/material.dart';

class PackageInfoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Practice - package_info'),
      ),
      body: Center(
        child: Text('Practice - package_info'),
      ),
    );
  }
}